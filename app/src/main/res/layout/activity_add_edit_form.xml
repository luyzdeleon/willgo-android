<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="edu.intec.willgo.AddEditActivity">


    <EditText
        android:id="@+id/EditTextName"
        android:layout_height="wrap_content"
        android:hint="Interes"
        android:inputType="textPersonName"
        android:layout_width="fill_parent">
    </EditText>

    <EditText
        android:id="@+id/EditTextLugar"
        android:layout_height="wrap_content"
        android:hint="Lugar (Pais)"
        android:inputType="textPersonName"
        android:layout_width="fill_parent">
    </EditText>

    <EditText
        android:id="@+id/EditTextUbicacion"
        android:layout_height="wrap_content"
        android:hint="Ubicacion (Direccion)"
        android:inputType="text"
        android:layout_width="fill_parent">
    </EditText>

        <Button
            android:id="@+id/ButtonSalvar"
            android:layout_height="wrap_content"
            android:text="Salvar"
            android:onClick="savePreference"
            android:layout_width="fill_parent"></Button>

        <Button
            android:id="@+id/ButtonCancelar"
            android:layout_height="wrap_content"
            android:text="Cancelar"
            android:onClick="cancelActivity"
            android:layout_width="fill_parent">
        </Button>

</TableLayout>

